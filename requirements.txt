# ── Protocol Buffers ─────────────────────────────────────────────
protobuf==4.25.3
grpcio-tools==1.62.1

# ── Sensor Integrations ──────────────────────────────────────────
# Base I2C / SPI / UART interfaces
smbus2==0.4.3

# Specific Adafruit standard hardware libraries
adafruit-blinka==8.38.0
adafruit-circuitpython-scd4x==1.4.13
adafruit-circuitpython-tsl2561==3.3.25
adafruit-circuitpython-tca9548a==0.8.6
adafruit-circuitpython-ads1x15==2.2.22

# Other community hardware drivers (MAX30100: core/max30100.py, no package needed)
RPi.bme280==0.2.4
pi-ina219==1.4.1

# ── ECLSS / EVA hardware (Phase 5) ───────────────────────────────
gpiozero==2.0.1; sys_platform == "linux"          # flow meter, PIR, relays (Pi 4 and 5)
lgpio==0.2.2.0; sys_platform == "linux"           # HX711 bit-banging (tight timing)
adafruit-circuitpython-pca9685==3.4.15            # tunable-white LED PWM
mfrc522==0.0.7; sys_platform == "linux"           # RC522 RFID over SPI (optional; USB readers use evdev)
pyserial==3.5                                      # GPS (NEO-M9N) and UWB (DWM1001) UARTs

# ── Core Edge Networking & Replay ───────────────────────────────
paho-mqtt==1.6.1
confluent-kafka==2.3.0
requests==2.31.0
PyYAML==6.0.1                                      # calibration.yaml

# ── Inventory (Phase 11) ─────────────────────────────────────────
# USB HID barcode scanner input (inventory/barcode_listener.py --device)
evdev==1.7.1; sys_platform == "linux"
